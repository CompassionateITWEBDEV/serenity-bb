registry=https://registry.npmjs.org/

# Allow peer dependency conflicts (needed for React 19 compatibility)
strict-peer-dependencies=false

prefer-offline=true
fetch-retries=5
fetch-retry-mintimeout=1000
fetch-retry-maxtimeout=60000
